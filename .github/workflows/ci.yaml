name: CI
on:
  # Pushes to any branch (here or on forks) run a skeleton: installs,
  # linting, and domainless tests. The full suite (the domain test jobs)
  # runs for pull requests, pushes to develop/main, and workflow_dispatch.
  # To ALSO run domain jobs on branch pushes, put a ci-<token> anywhere in
  # the branch name or in the pushed (head) commit message. Tokens:
  # ci-all, ci-sagehen, ci-hru1, ci-drb, ci-ucb, ci-fgr. For example, a
  # branch named my_feature_ci-fgr runs both fgr jobs on every push, with
  # no workflow edits to clean up before merging; a commit-message token
  # applies to its push only. Details in DEVELOPER.md under "CI".
  push:
    branches:
      - "*"
      - "!v[0-9]+.[0-9]+.[0-9]+*"
  pull_request:
    branches:
      - "*"
      - "!v[0-9]+.[0-9]+.[0-9]+*"

  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: "Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)"
        required: false
        default: false

# Cancel in-flight runs superseded by a newer push on the same ref, but
# let every run on the mainline branches finish
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_name != 'develop' && github.ref_name != 'main' }}

jobs:
  pws_install:
    name: standard installation
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Upgrade pip and install build and twine
        run: |
          python -m pip install --upgrade pip
          pip install wheel build twine importlib_metadata setuptools numpy

      - name: Standard installation
        run: |
          pip --verbose install .

      - name: Print pws version
        run: |
          python -c "import pywatershed; print(pywatershed.__version__)"

      - name: Build pywatershed, check dist outputs
        run: |
          python -m build
          twine check --strict dist/*

  pws_editable_install:
    name: editable installation
    runs-on: macos-latest
    strategy:
      fail-fast: false
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Upgrade pip and install build and twine
        run: |
          python -m pip install --upgrade pip
          pip install wheel build twine importlib_metadata setuptools numpy

      - name: Editable installation
        run: |
          pip --verbose install -e .

      - name: Print pws version
        run: |
          python -c "import pywatershed; print(pywatershed.__version__)"

      - name: Build pywatershed, check dist outputs
        run: |
          python -m build
          twine check --strict dist/*

  pyws_lint:
    name: linting
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          pip install wheel
          pip install .
          pip install ".[lint]"

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Lint
        run: ruff check .

      - name: Format
        run: ruff format --check .

  test_domainless:
    name: domainless ${{ matrix.os }} py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Set environment variables
        run: |
          echo "PYTHON_VERSION=${{ matrix.python-version }}" >> $GITHUB_ENV
          cat .mf6_ci_ref_remote  >> $GITHUB_ENV

      - name: Enforce MF6 ref and remote merge to main
        # need the code to update flopy but can get the binary
        # from nightly builds
        if: github.base_ref == 'main'
        run: |
          echo Merge commit: GITHUB_BASE_REF = $GITHUB_BASE_REF
          req_ref=develop  # if not develop, submit an issue
          echo $MF6_REF
          if [[ "$MF6_REF" != "$req_ref" ]]; then exit 1; fi
          req_remote=MODFLOW-USGS/modflow6
          echo $MF6_REMOTE
          if [[ "$MF6_REMOTE" != "$req_remote" ]]; then echo "bad mf6 remote in .mf6_ci_ref_remote"; exit 1; fi

      - name: Checkout MODFLOW 6
        uses: actions/checkout@v6
        with:
          repository: ${{ env.MF6_REMOTE }}
          ref: ${{ env.MF6_REF }}
          path: modflow6

      - name: Update flopy MODFLOW 6 classes
        working-directory: modflow6/autotest
        run: |
          # requires flopy[codegen] install option; --ref develop keeps
          # the generated classes in sync with the mf6 nightly binary
          # (flopy's bundled dfn snapshot lags mf6 develop)
          python -m flopy.mf6.utils.generate_classes --ref develop

      - name: Install mf6 nightly build binaries
        uses: modflowpy/install-modflow-action@v1
        with:
          repo: modflow6-nightly-build

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: domainless - run tests not requiring domain data
        working-directory: autotest
        run: pytest
          -m domainless
          -n=auto
          -vv
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_domainless.xml

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for domainless-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_domainless.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_sagehen_5yr:
    name: sagehen_5yr ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-sagehen') ||
      contains(github.event.head_commit.message, 'ci-sagehen')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Compile PRMS 5.2.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"

      - name: sagehen_5yr_no_cascades - generate and manage test data domain, run PRMS and convert csv output to NetCDF
        working-directory: autotest
        run: |
          python generate_test_data.py \
            --domain=sagehen_5yr \
            --control_pattern=sagehen_no_cascades.control \
            --remove_prms_csvs --remove_prms_output_dirs

      - name: sagehen_5yr_no_cascades - list netcdf input files
        working-directory: test_data
        run: |
          find sagehen_5yr/output_no_cascades -name '*.nc' | sort -n

      - name: sagehen_5yr_no_cascades - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=sagehen_5yr
          --control_pattern=sagehen_no_cascades.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_sagehen_5yr_no_cascades.xml
          --ignore=test_cbh_to_netcdf.py
          --ignore=test_prms_dyn_params.py
          --ignore=test_control_read.py
          --ignore=test_domain_subset.py
          --ignore=test_mmr_to_mf6_dfw.py
          --ignore=test_model.py
          --ignore=test_netcdf_subset.py
          --ignore=test_obsin_flow_node.py
          --ignore=test_output.py
          --ignore=test_pass_through_flow_graph.py
          --ignore=test_preprocess_cascades.py
          --ignore=test_prms_param_separate_cascades.py
          --ignore=test_prms_atmosphere_transp_frost.py
          --ignore=test_prms_atmosphere_transp_frost_dynamic.py
          --ignore=test_prms_channel.py
          --ignore=test_prms_channel_flow_graph.py
          --ignore=test_prms_et.py
          --ignore=test_prms_et_can_runoff.py
          --ignore=test_prms_et_canopy.py
          --ignore=test_prms_hydraulic_geometry.py
          --ignore=test_prms_runoff.py
          --ignore=test_prms_runoff_ag.py
          --ignore=test_prms_runoff_ag_restart.py
          --ignore=test_prms_runoff_soilzone_ag.py
          --ignore=test_prms_soilzone_ag.py
          --ignore=test_prms_soilzone_ag_restart.py
          --ignore=test_prms_stream_temp.py
          --ignore=test_source_sink_flow_node.py
          --ignore=test_starfit_flow_graph.py

      - name: sagehen_5yr_no_gw_cascades - generate and manage test data domain, run PRMS and convert csv output to NetCDF
        working-directory: autotest
        run: |
          python generate_test_data.py \
            --domain=sagehen_5yr \
            --control_pattern=sagehen_no_gw_cascades.control \
            --remove_prms_csvs --remove_prms_output_dirs

      - name: sagehen_5yr_no_gw_cascades - list netcdf input files
        working-directory: test_data
        run: |
          find sagehen_5yr/output_no_gw_cascades -name '*.nc' | sort -n

      - name: sagehen_5yr_no_gw_cascades - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=sagehen_5yr
          --control_pattern=sagehen_no_gw_cascades.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_sagehen_5yr_no_gw_cascades.xml
          test_preprocess_cascades.py
          test_prms_param_separate_cascades.py
          test_prms_above_snow.py
          test_prms_atmosphere.py
          test_prms_below_snow.py
          test_prms_canopy.py
          test_prms_groundwater.py
          test_restart_processes.py
          test_prms_snow.py
          test_prms_solar_geom.py
          test_self_drive.py

      - name: sagehen_5yr_cascades - generate and manage test data domain, run PRMS and convert csv output to NetCDF
        working-directory: autotest
        run: |
          python generate_test_data.py \
            --domain=sagehen_5yr \
            --control_pattern=sagehen.control \
            --remove_prms_csvs --remove_prms_output_dirs

      - name: sagehen_5yr_cascades - list netcdf input files
        working-directory: test_data
        run: |
          find sagehen_5yr/output -name '*.nc' | sort -n

      - name: sagehen_5yr_cascades - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=sagehen_5yr
          --control_pattern=sagehen.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_sagehen_5yr_cascades.xml
          test_preprocess_cascades.py
          test_prms_param_separate_cascades.py
          test_prms_above_snow.py
          test_prms_atmosphere.py
          test_prms_below_snow.py
          test_prms_canopy.py
          test_prms_groundwater.py
          test_restart_processes.py
          test_prms_snow.py
          test_prms_solar_geom.py
          test_self_drive.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for sagehen_5yr-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_sagehen_5yr_no_cascades.xml
            ./autotest/pytest_sagehen_5yr_no_gw_cascades.xml
            ./autotest/pytest_sagehen_5yr_cascades.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_sagehen_gridded_5yr:
    name: sagehen_gridded_5yr ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-sagehen') ||
      contains(github.event.head_commit.message, 'ci-sagehen')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Compile PRMS 5.2.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"

      - name: sagehen_gridded_5yr - generate CBH forcing files with PRMS
        working-directory: autotest
        run: |
          python generate_test_data.py \
            --domain=sagehen_gridded_5yr \
            --control_pattern=make_cbh_only

      - name: sagehen_gridded_5yr - generate and manage test data domain, run PRMS and convert csv output to NetCDF
        working-directory: autotest
        run: |
          python generate_test_data.py \
            --domain=sagehen_gridded_5yr \
            --control_pattern=sagehen_gridded_cascades.control \
            --remove_prms_csvs --remove_prms_output_dirs

      - name: sagehen_gridded_5yr - remove PRMS CBH day files (disk space)
        working-directory: test_data
        run: |
          # the .day files (~600 MB) drive the PRMS runs above; the tests
          # only read the netcdf CBH files (prcp/tmax/tmin.nc)
          rm -f sagehen_gridded_5yr/*.day
          df -h . || true

      - name: sagehen_gridded_5yr - list netcdf input files
        working-directory: test_data
        run: |
          find sagehen_gridded_5yr/output_cascades -name '*.nc' | sort -n

      - name: sagehen_gridded_5yr - pywatershed tests
        working-directory: autotest
        # -n limited (not auto): xdist workers x the gridded domain's memory
        # footprint OOMs the ubuntu runner (pytest killed, exit 143), even
        # at -n=2, so run serially there; other OSes are fine at -n=2
        run: pytest
          -vv
          -rs
          -n=${{ runner.os == 'Linux' && '1' || '2' }}
          -m "not domainless"
          --domain=sagehen_gridded_5yr
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_sagehen_gridded_5yr_cascades.xml
          test_prms_param_separate_cascades.py
          test_prms_above_snow.py
          test_prms_below_snow.py
          test_prms_groundwater.py
          test_restart_processes.py
          test_prms_snow.py
          test_prms_solar_geom.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for sagehen_gridded_5yr-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_sagehen_gridded_5yr_cascades.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_hru_1:
    name: hru_1 ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-hru1') ||
      contains(github.event.head_commit.message, 'ci-hru1')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Compile PRMS 5.2.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"

      - name: hru_1 all configs - generate and manage test data, run PRMS and convert csv output to NetCDF
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto --domain=hru_1 \
            --remove_prms_csvs --remove_prms_output_dirs \
            --control_pattern=nhm.control \
            --control_pattern=frost.control

      - name: hru_1_nhm - list netcdf input files
        working-directory: test_data
        run: |
          find hru_1/output -name '*.nc' | sort -n

      - name: hru_1_nhm_transp_frost - list netcdf input files
        working-directory: test_data
        run: |
          find hru_1/output_transp_frost -name '*.nc' | sort -n

      - name: hru_1_nhm - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=hru_1
          --control_pattern=nhm.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_hru_1_nhm.xml
          --ignore=test_domain_subset.py
          --ignore=test_prms_dyn_params.py
          --ignore=test_mmr_to_mf6_dfw.py
          --ignore=test_obsin_flow_node.py
          --ignore=test_output.py
          --ignore=test_pass_through_flow_graph.py
          --ignore=test_preprocess_cascades.py
          --ignore=test_prms_param_separate_cascades.py
          --ignore=test_prms_atmosphere_transp_frost.py
          --ignore=test_prms_atmosphere_transp_frost_dynamic.py
          --ignore=test_prms_channel_flow_graph.py
          --ignore=test_prms_hydraulic_geometry.py
          --ignore=test_prms_runoff_ag.py
          --ignore=test_prms_runoff_ag_restart.py
          --ignore=test_prms_runoff_soilzone_ag.py
          --ignore=test_prms_soilzone_ag.py
          --ignore=test_prms_soilzone_ag_restart.py
          --ignore=test_prms_stream_temp.py
          --ignore=test_source_sink_flow_node.py
          --ignore=test_starfit_flow_graph.py

      - name: hru_1_nhm_transp_frost - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=hru_1
          --control_pattern=nhm_transp_frost.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_hru_1_transp_frost.xml
          test_prms_atmosphere_transp_frost.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for hru_1-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_hru_1_nhm.xml
            ./autotest/pytest_hru_1_transp_frost.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_drb_2yr:
    name: drb_2yr ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-drb') ||
      contains(github.event.head_commit.message, 'ci-drb')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Set environment variables
        run: |
          echo "PYTHON_VERSION=${{ matrix.python-version }}" >> $GITHUB_ENV
          cat .mf6_ci_ref_remote  >> $GITHUB_ENV

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Enforce MF6 ref and remote merge to main
        # need the code to update flopy but can get the binary
        # from nightly builds
        if: github.base_ref == 'main'
        run: |
          echo Merge commit: GITHUB_BASE_REF = $GITHUB_BASE_REF
          req_ref=develop  # if not develop, submit an issue
          echo $MF6_REF
          if [[ "$MF6_REF" != "$req_ref" ]]; then exit 1; fi
          req_remote=MODFLOW-USGS/modflow6
          echo $MF6_REMOTE
          if [[ "$MF6_REMOTE" != "$req_remote" ]]; then echo "bad mf6 remote in .mf6_ci_ref_remote"; exit 1; fi

      - name: Checkout MODFLOW 6
        uses: actions/checkout@v6
        with:
          repository: ${{ env.MF6_REMOTE }}
          ref: ${{ env.MF6_REF }}
          path: modflow6

      - name: Update flopy MODFLOW 6 classes
        working-directory: modflow6/autotest
        run: |
          # requires flopy[codegen] install option; --ref develop keeps
          # the generated classes in sync with the mf6 nightly binary
          # (flopy's bundled dfn snapshot lags mf6 develop)
          python -m flopy.mf6.utils.generate_classes --ref develop

      - name: Install mf6 nightly build binaries
        uses: modflowpy/install-modflow-action@v1
        with:
          repo: modflow6-nightly-build

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Get GIS files for tests
        run: |
          python pywatershed/utils/gis_files.py

      - name: Compile PRMS 5.2.1 and 5.2.1.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1.1')"

      - name: drb_2yr all domains - generate and manage test data
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto --domain=drb_2yr \
            --remove_prms_csvs --remove_prms_output_dirs

      - name: drb_2yr all domains - list netcdf input files
        working-directory: test_data
        run: |
          find drb_2yr/output*/ -name '*.nc' | sort -n

      - name: drb_2yr_nhm - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=drb_2yr
          --control_pattern=nhm.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_drb_2yr_nhm.xml
          --ignore=test_obsin_flow_node.py
          --ignore=test_preprocess_cascades.py
          --ignore=test_prms_param_separate_cascades.py
          --ignore=test_prms_dyn_params.py
          --ignore=test_prms_atmosphere_transp_frost.py
          --ignore=test_prms_atmosphere_transp_frost_dynamic.py
          --ignore=test_prms_hydraulic_geometry.py
          --ignore=test_prms_stream_temp.py
          --ignore=test_domain_subset.py
          --ignore=test_prms_runoff_ag.py
          --ignore=test_prms_runoff_ag_restart.py
          --ignore=test_prms_runoff_soilzone_ag.py
          --ignore=test_prms_soilzone_ag.py
          --ignore=test_prms_soilzone_ag_restart.py
          --ignore=test_source_sink_flow_node.py
          --ignore=test_starfit_flow_graph.py

      # Specific tests not redundant with dprst
      - name: drb_2yr_no_dprst - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=drb_2yr
          --control_pattern=no_dprst
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_drb_2yr_no_dprst.xml
          test_prms_runoff.py
          test_prms_soilzone.py
          test_prms_groundwater.py
          test_prms_above_snow.py
          test_prms_below_snow.py
          test_restart_processes.py

      # Specific tests not redundant with dprst
      - name: drb_2yr_obsin - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=0
          -m "not domainless"
          --domain=drb_2yr
          --control_pattern=nhm_obsin.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_drb_2yr_obsin.xml
          test_obsin_flow_node.py

      # Specific tests not redundant with dprst
      - name: drb_2yr_nhm_transp_frost - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=drb_2yr
          --control_pattern=nhm_transp_frost.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_drb_2yr_transp_frost.xml
          test_prms_atmosphere_transp_frost.py

      # Specific tests not redundant with dprst
      - name: drb_2yr_stream_temp - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=drb_2yr
          --control_pattern=nhm_stream_temp
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_drb_2yr_stream_temp.xml
          test_prms_hydraulic_geometry.py
          test_prms_stream_temp.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for drb_2yr-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_drb_2yr_nhm.xml
            ./autotest/pytest_drb_2yr_no_dprst.xml
            ./autotest/pytest_drb_2yr_obsin.xml
            ./autotest/pytest_drb_2yr_transp_frost.xml
            ./autotest/pytest_drb_2yr_stream_temp.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_ucb_2yr_nhm:
    name: ucb_2yr_nhm ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-ucb') ||
      contains(github.event.head_commit.message, 'ci-ucb')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Set environment variables
        run: |
          echo "PYTHON_VERSION=${{ matrix.python-version }}" >> $GITHUB_ENV
          cat .mf6_ci_ref_remote  >> $GITHUB_ENV

      - name: Enforce MF6 ref and remote merge to main
        if: github.base_ref == 'main'
        run: |
          echo Merge commit: GITHUB_BASE_REF = $GITHUB_BASE_REF
          req_ref=develop  # if not develop, submit an issue
          echo $MF6_REF
          if [[ "$MF6_REF" != "$req_ref" ]]; then exit 1; fi
          req_remote=MODFLOW-USGS/modflow6
          echo $MF6_REMOTE
          if [[ "$MF6_REMOTE" != "$req_remote" ]]; then echo "bad mf6 remote in .mf6_ci_ref_remote"; exit 1; fi

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Compile PRMS 5.2.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"

      - name: ucb_2yr_nhm - generate and manage test data
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto \
            --domain=ucb_2yr \
            --remove_prms_csvs \
            --remove_prms_output_dirs \
            --control_pattern=nhm.control

      - name: ucb_2yr_nhm - list netcdf input files
        working-directory: test_data
        run: |
          find ucb_2yr/output -name '*.nc' | sort -n

      - name: ucb_2yr_nhm - pywatershed tests (subset 1)
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=ucb_2yr
          --control_pattern=nhm.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_ucb_2yr_nhm.xml
          --ignore=test_netcdf_subset.py
          --ignore=test_prms_dyn_params.py
          --ignore=test_obsin_flow_node.py
          --ignore=test_output.py
          --ignore=test_pass_through_flow_graph.py
          --ignore=test_preprocess_cascades.py
          --ignore=test_prms_param_separate_cascades.py
          --ignore=test_prms_atmosphere_transp_frost.py
          --ignore=test_prms_atmosphere_transp_frost_dynamic.py
          --ignore=test_mmr_to_mf6_dfw.py
          --ignore=test_prms_hydraulic_geometry.py
          --ignore=test_prms_stream_temp.py
          --ignore=test_prms_runoff_ag.py
          --ignore=test_prms_runoff_ag_restart.py
          --ignore=test_prms_runoff_soilzone_ag.py
          --ignore=test_prms_soilzone_ag.py
          --ignore=test_prms_soilzone_ag_restart.py
          --ignore=test_prms_soilzone.py
          --ignore=test_prms_runoff.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for ucb_2yr_nhm-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_ucb_2yr_nhm.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_ucb_2yr_slow_tests:
    name: ucb_2yr_slow_tests ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-ucb') ||
      contains(github.event.head_commit.message, 'ci-ucb')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Compile PRMS 5.2.1
        run: |
          which gfortran
          gfortran --version
          which make
          python -c "import pywatershed as pws; pws.utils.compile_prms('5.2.1')"

      - name: ucb_2yr_slow_tests - generate and manage test data
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto \
            --domain=ucb_2yr \
            --remove_prms_csvs \
            --remove_prms_output_dirs \
            --control_pattern=nhm.control \
            --control_pattern=transp_frost.control

      - name: ucb_2yr_slow_tests - list netcdf input files
        working-directory: test_data
        run: |
          find ucb_2yr/output -name '*.nc' | sort -n
          find ucb_2yr/output_transp_frost -name '*.nc' | sort -n

      - name: ucb_2yr_slow_tests - pywatershed tests (subset 2)
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          --domain=ucb_2yr
          --control_pattern=nhm.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_ucb_2yr_slow_tests.xml
          test_prms_soilzone.py
          test_prms_runoff.py

      - name: ucb_2yr_transp_frost - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          -m "not domainless"
          --domain=ucb_2yr
          --control_pattern=nhm_transp_frost.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-append
          --cov-report=xml
          --junitxml=pytest_ucb_2yr_transp_frost.xml
          test_prms_atmosphere_transp_frost.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for ucb_2yr_slow_tests-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_ucb_2yr_slow_tests.xml
            ./autotest/pytest_ucb_2yr_transp_frost.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_fgr_ag_2yr_subset1:
    name: fgr_ag_2yr_subset1 ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-fgr') ||
      contains(github.event.head_commit.message, 'ci-fgr')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Get additional domain files for tests
        run: |
          python pywatershed/utils/addtl_domain_files.py
          if [ $? -ne 0 ]; then
            echo "Failed to download additional domain files"
            exit 1
          fi
          echo ""
          echo "Checking where additional domains were downloaded:"
          python -c "
          import pywatershed as pws
          pkg_root = pws.constants.__pywatershed_root__
          addtl_dir = pkg_root / 'data' / 'pywatershed_addtl_domains'
          print(f'Package root: {pkg_root}')
          print(f'Additional domains dir: {addtl_dir}')
          print(f'Additional domains exists: {addtl_dir.exists()}')
          if addtl_dir.exists():
              import os
              print(f'Contents: {os.listdir(addtl_dir)}')
          "

      - name: Create symlink to fgr_ag_2yr domain
        run: |
          echo "Current directory: $(pwd)"
          echo ""
          python -c "
          import os
          import sys
          import platform
          import shutil
          # Remove current directory from sys.path to avoid importing local source
          # We want the installed package in site-packages, not repo source
          if '' in sys.path:
              sys.path.remove('')
          if '.' in sys.path:
              sys.path.remove('.')
          cwd = os.getcwd()
          if cwd in sys.path:
              sys.path.remove(cwd)
          # Now import pywatershed from site-packages
          import pywatershed as pws
          # Find pywatershed package location using __file__
          pkg_dir = os.path.dirname(pws.__file__)
          src = os.path.join(pkg_dir, 'data', 'pywatershed_addtl_domains', 'fgr_ag_2yr')
          dst = 'test_data/fgr_ag_2yr'
          print(f'Platform: {platform.system()}')
          print(f'Package directory: {pkg_dir}')
          print(f'Source path: {src}')
          print(f'Source exists: {os.path.exists(src)}')
          print(f'Destination path: {dst}')
          print(f'Destination absolute: {os.path.abspath(dst)}')
          if not os.path.exists(src):
              print(f'ERROR: Source path does not exist!')
              print(f'Checking pywatershed/data:')
              data_dir = os.path.join(pkg_dir, 'data')
              if os.path.exists(data_dir):
                  print(f'Contents: {os.listdir(data_dir)}')
              sys.exit(1)
          if os.path.exists(dst):
              print(f'Removing existing destination')
              if os.path.islink(dst) or os.path.isfile(dst):
                  os.unlink(dst)
              else:
                  shutil.rmtree(dst)
          # On Windows, copy instead of symlink (symlinks require special privileges)
          if platform.system() == 'Windows':
              print(f'Copying directory (Windows)')
              shutil.copytree(src, dst)
          else:
              print(f'Creating symlink (Unix)')
              os.symlink(src, dst, target_is_directory=True)
          print(f'Link/copy created successfully')
          print(f'Destination exists (after): {os.path.exists(dst)}')
          "

      - name: fgr_ag_2yr all configs - generate and manage test data
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto \
            --domain=fgr_ag_2yr \
            --remove_prms_csvs \
            --remove_prms_output_dirs \
            --control_pattern=spinup.control \
            --control_pattern=analysis.control

      - name: fgr_ag_2yr_subset1 - list netcdf input files
        working-directory: test_data
        run: |
          find fgr_ag_2yr/output_spinup -name '*.nc' | sort -n
          find fgr_ag_2yr/output_analysis -name '*.nc' | sort -n

      - name: fgr_ag_2yr_subset1 - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          --domain=fgr_ag_2yr
          --control_pattern=spinup.control
          --control_pattern=analysis.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_fgr_ag_2yr_subset1.xml
          test_prms_runoff_ag.py
          test_prms_runoff_ag_restart.py
          test_prms_soilzone_ag.py
          test_prms_soilzone_ag_restart.py
          test_prms_dyn_params.py

      # analysis.control only: these tests skip without dynamic frost dates
      - name: fgr_ag_2yr_analysis transp_frost_dynamic - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          --domain=fgr_ag_2yr
          --control_pattern=analysis.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_fgr_ag_2yr_transp_frost_dynamic.xml
          test_prms_atmosphere_transp_frost_dynamic.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for fgr_ag_2yr_subset1-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_fgr_ag_2yr_subset1.xml
            ./autotest/pytest_fgr_ag_2yr_transp_frost_dynamic.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"

  test_fgr_ag_2yr_subset2:
    name: fgr_ag_2yr_subset2 ${{ matrix.os }} py${{ matrix.python-version }}
    # domain jobs skip plain branch pushes; see the ci-<token> note
    # in the trigger comment at the top of this file
    if: >-
      github.event_name != 'push' ||
      github.ref_name == 'develop' ||
      github.ref_name == 'main' ||
      contains(github.ref_name, 'ci-all') ||
      contains(github.event.head_commit.message, 'ci-all') ||
      contains(github.ref_name, 'ci-fgr') ||
      contains(github.event.head_commit.message, 'ci-fgr')
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Install Dependencies via Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          log-level: debug
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          create-args: >-
            python=${{matrix.python-version}}

      - name: Link conda gfortran dylibs on Mac
        if: runner.os == 'macOS'
        run: .github/scripts/symlink_gfortran_mac.sh

      - name: Install pywatershed
        run: |
          pip install .

      - name: Version info
        run: |
          pip -V
          pip list

      - name: Get additional domain files for tests
        run: |
          python pywatershed/utils/addtl_domain_files.py
          if [ $? -ne 0 ]; then
            echo "Failed to download additional domain files"
            exit 1
          fi
          echo ""
          echo "Checking where additional domains were downloaded:"
          python -c "
          import pywatershed as pws
          pkg_root = pws.constants.__pywatershed_root__
          addtl_dir = pkg_root / 'data' / 'pywatershed_addtl_domains'
          print(f'Package root: {pkg_root}')
          print(f'Additional domains dir: {addtl_dir}')
          print(f'Additional domains exists: {addtl_dir.exists()}')
          if addtl_dir.exists():
              import os
              print(f'Contents: {os.listdir(addtl_dir)}')
          "

      - name: Create symlink to fgr_ag_2yr domain
        run: |
          echo "Current directory: $(pwd)"
          echo ""
          python -c "
          import os
          import sys
          import platform
          import shutil
          # Remove current directory from sys.path to avoid importing local source
          # We want the installed package in site-packages, not repo source
          if '' in sys.path:
              sys.path.remove('')
          if '.' in sys.path:
              sys.path.remove('.')
          cwd = os.getcwd()
          if cwd in sys.path:
              sys.path.remove(cwd)
          # Now import pywatershed from site-packages
          import pywatershed as pws
          # Find pywatershed package location using __file__
          pkg_dir = os.path.dirname(pws.__file__)
          src = os.path.join(pkg_dir, 'data', 'pywatershed_addtl_domains', 'fgr_ag_2yr')
          dst = 'test_data/fgr_ag_2yr'
          print(f'Platform: {platform.system()}')
          print(f'Package directory: {pkg_dir}')
          print(f'Source path: {src}')
          print(f'Source exists: {os.path.exists(src)}')
          print(f'Destination path: {dst}')
          print(f'Destination absolute: {os.path.abspath(dst)}')
          if not os.path.exists(src):
              print(f'ERROR: Source path does not exist!')
              print(f'Checking pywatershed/data:')
              data_dir = os.path.join(pkg_dir, 'data')
              if os.path.exists(data_dir):
                  print(f'Contents: {os.listdir(data_dir)}')
              sys.exit(1)
          if os.path.exists(dst):
              print(f'Removing existing destination')
              if os.path.islink(dst) or os.path.isfile(dst):
                  os.unlink(dst)
              else:
                  shutil.rmtree(dst)
          # On Windows, copy instead of symlink (symlinks require special privileges)
          if platform.system() == 'Windows':
              print(f'Copying directory (Windows)')
              shutil.copytree(src, dst)
          else:
              print(f'Creating symlink (Unix)')
              os.symlink(src, dst, target_is_directory=True)
          print(f'Link/copy created successfully')
          print(f'Destination exists (after): {os.path.exists(dst)}')
          "

      - name: fgr_ag_2yr_subset2 all configs - generate and manage test data
        working-directory: autotest
        run: |
          python generate_test_data.py \
            -n=auto \
            --domain=fgr_ag_2yr \
            --remove_prms_csvs \
            --remove_prms_output_dirs \
            --control_pattern=spinup.control \
            --control_pattern=analysis.control

      - name: fgr_ag_2yr_subset2 - list netcdf input files
        working-directory: test_data
        run: |
          find fgr_ag_2yr/output_spinup -name '*.nc' | sort -n
          find fgr_ag_2yr/output_analysis -name '*.nc' | sort -n

      - name: fgr_ag_2yr_subset2 - pywatershed tests
        working-directory: autotest
        run: pytest
          -vv
          -rs
          -n=auto
          --domain=fgr_ag_2yr
          --control_pattern=spinup.control
          --control_pattern=analysis.control
          --durations=0
          --error-for-skips
          --cov=pywatershed
          --cov-report=xml
          --junitxml=pytest_fgr_ag_2yr_subset2.xml
          test_prms_runoff_soilzone_ag.py

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: Test results for fgr_ag_2yr_subset2-${{ runner.os }}-${{ matrix.python-version }}
          path: |
            ./autotest/pytest_fgr_ag_2yr_subset2.xml

      - name: Upload code coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./autotest/coverage.xml
          env_vars: RUNNER_OS,PYTHON_VERSION
          fail_ci_if_error: false
          version: "v0.1.15"
